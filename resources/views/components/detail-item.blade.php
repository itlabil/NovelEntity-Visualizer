@props(['label', 'value'])
<div>
    <p class="text-[10px] text-gray-500 uppercase font-bold tracking-tight">{{ $label }}</p>
    <p class="text-sm font-semibold text-gray-800">{{ $value }}</p>
</div>